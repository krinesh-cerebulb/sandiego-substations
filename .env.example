# Mapbox GL public access token (starts with "pk."). Get one at
# https://account.mapbox.com/access-tokens/
# Copy this file to `.env` and fill in the value, then restart `pnpm dev`.
VITE_MAPBOX_TOKEN=
